require 'fileutils'
require 'rake'

desc "deploy to to ripple c2 (https)"
task :c2 do
    sh "cap deploy -S target=ripple:/opt/web-services/sites/simple-put"
end

desc "deploy to to ripple c3 (https)"
task :c3 do
    sh "cap deploy -S target=ripple:/opt/web-services/sites/simple-put"
end
